<% if user_signed_in? %>
  <canvas id="map" width="512" height="480" >
    Looks like your browser doesn't support canvas.
  </canvas>
<% else %>
  <p>
    Sign In or Sign Up to play!
  </p>
<% end %>